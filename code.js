var graph1 = {'foo': {'boo': 7},
             'boo': {'foo': 3, 'bar': 2},
             'bar': {'boo': 4}};

function augmentingPath(graph, start, end) {
    // Hi Lars, do you mean I need convert the graph to adjacency list first?
    // for example, when take graph1 as a parameter, to convert to a adjacency list first?
    return [];
}
augmentingPath(graph1, 'foo', 'bar')
